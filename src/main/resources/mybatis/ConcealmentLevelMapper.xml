<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lvwyh.mapper.ConcealmentLevelMapper">
    <resultMap id="ConcealmentLevelResult" type="com.lvwyh.entity.ConcealmentLevel">
        <id property="id" column="id" />
        <result property="level" column="level" />
        <result property="discoverRadius" column="discover_radius" />
        <result property="airDiscoverRadius" column="air_discover_radius" />
    </resultMap>

    <insert id="insert" parameterType="com.lvwyh.entity.ConcealmentLevel" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO concealment_level (level, discover_radius, air_discover_radius)
        VALUES (#{level}, #{discoverRadius}, #{airDiscoverRadius})
    </insert>

    <select id="selectByLevel" resultMap="ConcealmentLevelResult">
        SELECT id, level, discover_radius, air_discover_radius
        FROM concealment_level
        WHERE level = #{level}
    </select>

    <select id="selectAll" resultMap="ConcealmentLevelResult">
        SELECT id, level, discover_radius, air_discover_radius
        FROM concealment_level
        ORDER BY level ASC
    </select>

    <update id="update" parameterType="com.lvwyh.entity.ConcealmentLevel">
        UPDATE concealment_level
        SET level = #{level},
            discover_radius = #{discoverRadius},
            air_discover_radius = #{airDiscoverRadius}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM concealment_level WHERE id = #{id}
    </delete>
</mapper>
