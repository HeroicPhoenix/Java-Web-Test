<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lvwyh.mapper.ArmorLevelMapper">
    <resultMap id="ArmorLevelResult" type="com.lvwyh.entity.ArmorLevel">
        <id property="id" column="id" />
        <result property="level" column="level" />
        <result property="name" column="name" />
        <result property="startNum" column="start_num" />
        <result property="endNum" column="end_num" />
    </resultMap>

    <insert id="insert" parameterType="com.lvwyh.entity.ArmorLevel" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO armor_level (level, name, start_num, end_num)
        VALUES (#{level}, #{name}, #{startNum}, #{endNum})
    </insert>

    <select id="selectByLevel" resultMap="ArmorLevelResult">
        SELECT id, level, name, start_num, end_num
        FROM armor_level
        WHERE level = #{level}
    </select>

    <select id="selectAll" resultMap="ArmorLevelResult">
        SELECT id, level, name, start_num, end_num
        FROM armor_level
        ORDER BY level ASC
    </select>

    <update id="update" parameterType="com.lvwyh.entity.ArmorLevel">
        UPDATE armor_level
        SET level = #{level},
            name = #{name},
            start_num = #{startNum},
            end_num = #{endNum}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM armor_level WHERE id = #{id}
    </delete>
</mapper>
