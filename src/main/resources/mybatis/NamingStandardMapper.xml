<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lvwyh.mapper.NamingStandardMapper">

    <select id="selectNamingRule" resultType="com.lvwyh.entity.NamingStandardRule">
        SELECT id, rule_type, rule_code, rule_name, pattern, description
        FROM naming_standard_rule
        WHERE rule_code = #{ruleCode}
    </select>

    <select id="selectAdaptationRule" resultType="com.lvwyh.entity.NamingAdaptationRule">
        SELECT id, rule_code, rule_name, prefix, suffix, description
        FROM naming_adaptation_rule
        WHERE rule_code = #{ruleCode}
    </select>
</mapper>
